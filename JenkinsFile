pipeline {
  agent any

  triggers {
    cron('20 23 * * *')  // Runs daily at 1 AM, staggered start
  }

  tools {
    maven 'MAVEN_HOME'     // Jenkins tool config: Maven
    jdk 'JAVA_HOME'            // Jenkins tool config: JDK 21
  }

  environment {
    REPORT_DIR = 'test-output'
    REPORT_NAME = 'ExtentReport.html'
    PROFILE = 'smoke'
  }

  stages {
    stage('Checkout') {
        steps {
            git 'https://github.com/shashushashankk/Zood_Tech.git'
        }
    }

    stage('Build') {
        steps {
            bat 'mvn clean compile'
        }
    }

    stage('Test') {
        steps {
             bat 'mvn test -Psmoke'
        }
    }

    stage('Publish Extent Report') {
      steps {
        publishHTML([
          allowMissing: false,
          alwaysLinkToLastBuild: true,
          keepAll: true,
          reportDir: "${REPORT_DIR}",
          reportFiles: "${REPORT_NAME}",
          reportName: 'Test Execution Report'
        ])
      }
    }
  }

  post {
    always {
        junit '**/target/surefire-reports/*.xml'
    }
  }


//   post {
//     failure {
//       echo '🚨 Test run failed!'
//     }
//     success {
//       echo '✅ Tests completed successfully!'
//     }
//   }
}

// Field	Description	Example
//    MINUTE	0–59	0
//    HOUR	0–23	10
//    DOM	Day of month 1–31	*
//    MONTH	1–12 or JAN–DEC	*
//    DOW	0–7 or SUN–SAT	1-5 (Mon–Fri)